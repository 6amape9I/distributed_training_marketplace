# Orchestrator Scaffold

The orchestrator owns off-chain coordination in later stages. Stage 1 only provides a FastAPI shell, typed protocol interfaces, and shared domain structures so protocol logic can be added without restructuring the repository.
